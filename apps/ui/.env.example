# apps/api's base URL (job submit/status/download). Defaults to localhost:8080 if unset.
VITE_API_BASE_URL=http://localhost:8080

# apps/worker's base URL (sheet preview only). Defaults to localhost:4000 if unset.
VITE_WORKER_BASE_URL=http://localhost:4000
