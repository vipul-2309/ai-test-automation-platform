# @github/copilot-sdk needs Promise.withResolvers, only available from Node
# ^20.19.0 / >=22.12.0 (see package.json "engines"). engine-strict turns a silent
# EBADENGINE warning into a hard install failure, since the warning alone was easy
# to miss and led straight to a confusing runtime crash instead.
engine-strict=true
